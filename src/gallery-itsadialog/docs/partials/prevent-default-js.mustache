YUI(yuiconfig).use('gallery-itsadialog', function(Y) {

    Y.on('newmessage', function(e) {
        console.log('BEFORE');
    });

    Y.after('newmessage', function(e) {
        console.log('AFTER');
    });



    Y.ITSAMessageController.showMessage('Trying to prevent', 'Eerste bericht', {
        imageButtons: true
    }).then(
        function(result) {
            console.log('Eerste bericht fulfilled '+result);
        },
        function(reason) {
            console.log('Eerste bericht rejected '+reason);
        }
    );


});
