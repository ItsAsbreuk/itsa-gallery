YUI(yuiconfig).use('gallery-itsadialog', function(Y) {
    Y.showStatus('Status', 'Busy', {
        imageButtons: true
    }).then(
        function(itsamessage) {
            console.log('STATUS IS SET');
            Y.later(3000, null, function() {
                itsamessage.erase();
            });
        }
    );

    Y.showMessage('nieuwe titel 2', 'Volgend bericht', {
  //      primaryButton: 'imgbtn_ignore',
        imageButtons: true
    }).then(
        function(result) {
            console.log('Volgend bericht fulfilled '+result.button);
        },
        function(reason) {
            console.log('Volgend bericht rejected '+reason);
        }
    );

});
