YUI(yuiconfig).use('node-base', 'gallery-itsaformelement', function(Y) {
    var nodeId, itsaformelement, formElement;

    nodeId = Y.guid();
    itsaformelement = new Y.ITSAFormElement();
    formElement = itsaformelement.getElementPromise({type: 'input', value: 'default name'}, nodeId);

    formElement.then(
        function(element) {
            Y.one('#myform').append(element.html);
        }
    );
});
